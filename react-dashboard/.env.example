# Dev local: define a URL do backend (build-time Vite var).
# Docker/prod: deixe vazio — o nginx usa BACKEND_URL para proxy em runtime.
VITE_API_URL=http://localhost:8000

# Usado pelo nginx em runtime (docker run -e BACKEND_URL=http://backend:8000)
# BACKEND_URL=http://backend:8000
